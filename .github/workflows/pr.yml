name: PR

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static:
    name: Lint & typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          test-user-password: ${{ secrets.TEST_USER_PASSWORD }}
      - run: npm run lint
      - run: npm run type-check

  smoke:
    name: Smoke suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          test-user-password: ${{ secrets.TEST_USER_PASSWORD }}
      - run: npx playwright install --with-deps chromium
      - name: Start the SUT
        run: npm run sut:up
      - run: npm run test:smoke
      - name: Stop the SUT
        if: always()
        run: npm run sut:down
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
      - name: Link report in job summary
        if: always()
        run: echo "### 🎭 [Playwright report](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts) — download \`playwright-report\` artifact" >> "$GITHUB_STEP_SUMMARY"
